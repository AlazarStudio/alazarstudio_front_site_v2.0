# 🎯 Кастомный Желейный Курсор - Готовая Компонента

Самостоятельная React компонента кастомного курсора с деформацией от скорости.

## 📦 Установка

### Скопируйте 2 файла в ваш проект:

```
your-project/
├── src/
│   ├── components/
│   │   ├── CustomCursor.jsx    ← Скопируйте этот файл
│   │   └── CustomCursor.css    ← И этот файл
```

## 🚀 Быстрый старт

### 1. Импортируйте компонент в корень приложения

```jsx
// App.jsx
import CustomCursor from './components/CustomCursor';

function App() {
  return (
    <>
      <CustomCursor />
      {/* Ваш контент */}
    </>
  );
}

export default App;
```

### 2. Используйте data-атрибуты на элементах

```jsx
// Ссылки и кнопки
<a href="#" data-cursor="link">Ссылка</a>
<button data-cursor="link">Кнопка</button>

// Медиа с кастомной подписью
<div data-cursor="media" data-cursor-label="Play Video">
  Видео
</div>

// Слайдер (стрелки влево/вправо)
<div data-cursor="slider">
  Слайдер
</div>

// Перетаскиваемый элемент
<div data-cursor="drag">
  Drag me
</div>

// Скрыть курсор
<input data-cursor="hidden" type="text" />
```

## 🎨 Настройка

### Изменить цвет курсора

В файле `CustomCursor.css` измените переменную:

```css
:root {
  --cursor-bg: #fff;  /* Измените на любой цвет */
}
```

Примеры:
- `#000` - чёрный
- `#667eea` - фиолетовый
- `#ff6b6b` - красный

### Изменить размеры

В файле `CustomCursor.jsx` найдите объект `CONFIG`:

```javascript
const CONFIG = {
  size: {
    default: 25,  // Базовый размер
    link: 50,     // На ссылках
    media: 80,    // На медиа
  },
  // ...
};
```

### Изменить поведение

```javascript
const CONFIG = {
  // ...
  speed: 0.25,              // Плавность (0-1, меньше = плавнее)
  velocityMultiplier: 4,    // Чувствительность к скорости
  squeezeStrength: 0.5,     // Сила растяжения (0-0.5)
  rotationThreshold: 10,    // Порог для поворота
};
```

## 📋 Режимы курсора

| Режим | Атрибут | Описание |
|-------|---------|----------|
| **Default** | - | Базовый режим с деформацией |
| **Link** | `data-cursor="link"` | Увеличение на ссылках/кнопках |
| **Media** | `data-cursor="media"` | Большой круг с иконкой play |
| **Slider** | `data-cursor="slider"` | Стрелки влево/вправо |
| **Drag** | `data-cursor="drag"` | Подпись "Drag" |
| **Hidden** | `data-cursor="hidden"` | Скрыть курсор |

### Медиа режим с кастомной подписью

```jsx
<div 
  data-cursor="media" 
  data-cursor-label="Watch Now"
>
  Видео
</div>
```

## ✨ Возможности

- ✅ **Деформация от скорости** - растягивается при быстром движении
- ✅ **Поворот по направлению** - автоматически поворачивается
- ✅ **Уменьшение при скорости** - становится меньше при быстром движении
- ✅ **Плавные переходы** - все анимации с easing
- ✅ **Mix-blend-mode** - инверсия цветов под курсором
- ✅ **Автоотключение** на touch-устройствах
- ✅ **Поддержка prefers-reduced-motion**

## 🎯 Примеры использования

### Навигация

```jsx
<nav>
  <a href="/" data-cursor="link">Главная</a>
  <a href="/about" data-cursor="link">О нас</a>
  <a href="/contact" data-cursor="link">Контакты</a>
</nav>
```

### Галерея

```jsx
<div className="gallery">
  <div 
    className="video-item" 
    data-cursor="media" 
    data-cursor-label="Showreel"
  >
    <video src="video1.mp4" />
  </div>
  <div 
    className="video-item" 
    data-cursor="media" 
    data-cursor-label="Play"
  >
    <video src="video2.mp4" />
  </div>
</div>
```

### Слайдер

```jsx
<div className="slider" data-cursor="slider">
  <div className="slides">
    {/* Слайды */}
  </div>
</div>
```

### Формы

```jsx
<form>
  <input 
    type="text" 
    data-cursor="hidden" 
    placeholder="Имя"
  />
  <textarea 
    data-cursor="hidden"
    placeholder="Сообщение"
  />
  <button data-cursor="link">
    Отправить
  </button>
</form>
```

## 🔧 Требования

- React 16.8+ (хуки)
- Современный браузер с поддержкой CSS transforms

## 📝 Лицензия

MIT - используйте свободно в любых проектах!

---

**Создано с ❤️ для простой интеграции в любой React проект**

